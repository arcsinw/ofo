<Page
    x:Class="Ofo.View.UserAgreementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ofo.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource ImageButtonStyle}" Height="48" Width="48">
                    <Image Source="/Assets/actionbar_icon_close.png" Height="24" Width="24"/>
                </Button>
                <TextBlock Text="用户注册协议" VerticalAlignment="Center" FontSize="18"
                           Margin="12,0"/>
            </StackPanel>
        </Grid>

        <WebView Grid.Row="1"/>
    </Grid>
</Page>
