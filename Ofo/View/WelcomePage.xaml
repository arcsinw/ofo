<Page
    x:Class="Ofo.View.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ofo.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
 

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <MediaElement Source="ms-appx:///Assets/splash_video.mp4" IsLooping="True" Stretch="UniformToFill"/>

        <StackPanel HorizontalAlignment="Stretch"
                VerticalAlignment="Bottom"
                Margin="0,72">
            <!--<FontIcon FontFamily="{StaticResource IconFont}" 
                  Glyph="&#xe7ed;" 
                  Foreground="White" 
                  FontSize="140" />-->
            <Image Source="/Assets/img_splash_logo.png" Height="100" Margin="0,0,0,32"/>
            <Button Background="{StaticResource StandardLinearBrush}" Content="注册/登录" 
                Style="{StaticResource StandardButtonStyle}"
                HorizontalAlignment="Stretch"
                MinWidth="220"
                Margin="48,0"
                Click="NavigateToLoginPage"/>
        </StackPanel>
    </Grid>
</Page>
